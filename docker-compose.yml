services:
  app:
    build: .
    working_dir: /app
    env_file:
      - .env
    environment:
      APP_HOST: 0.0.0.0
      APP_ALLOW_CONTAINER_BIND: "true"
      APP_PORT: 8000
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/app/data
